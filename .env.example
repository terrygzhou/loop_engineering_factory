# Database credentials (for generated projects, not Loop Factory itself)
DB_PASSWORD=postgres

# OpenHands agent-server secret
OH_SECRET_KEY=changeme

# LLM API key (set to actual key for production)
LLM_API_KEY=not-needed
